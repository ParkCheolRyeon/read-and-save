[
  {
    "question": "43. React를 왜 선언형이라고 부르나",
    "answer": "React는 DOM을 직접 조작하는 게 아니라, **현재 상태에서 어떤 UI가 나와야 하는지를 선언**하는 모델이에요. 공식으로 표현하면 `UI = f(state)`인데, 명령형 방식처럼 DOM을 직접 찾아서 바꾸는 게 아니라 **상태와 UI의 대응 관계를 기술**하는 거죠. 상태가 바뀌면 React가 알아서 UI를 다시 계산하고, 이전 결과와 비교해서 **실제로 달라진 DOM만 최소한으로 반영**합니다. 이 덕분에 상태와 화면이 어긋나는 버그가 크게 줄어들고, 디버깅할 때도 DOM이 아니라 상태만 추적하면 됩니다.\n\n> React가 선언형이라는 건 알았다. **상태가 바뀌었을 때 React는 내부에서 어떤 단계를 거쳐 UI를 바꾸는가?**\n\n```js\n// 명령형: DOM을 직접 단계별로 조작\nconst p = document.getElementById(\"count-display\");\nconst btn = document.getElementById(\"increment-btn\");\nlet count = 0;\n\nbtn.addEventListener(\"click\", () => {\n  count++;\n  p.textContent = `현재 값: ${count}`; // 직접 DOM을 찾아서 바꾼다\n});\n```\n\n---\n\n**⚠️ 자주 하는 오해**\n\n- 선언형이면 내부적으로 아무 일도 안 한다 → **틀림.** React는 Virtual DOM 비교, DOM 조작을 모두 수행한다. 개발자가 직접 안 할 뿐.\n- React는 DOM을 안 만진다 → **틀림.** 계산된 결과를 바탕으로 최소한의 DOM 변경을 수행한다.\n- 선언형이면 성능이 더 좋다 → **항상 그런 건 아니다.** 선언형의 장점은 성능보다 **예측 가능성과 유지보수성**이다.\n\n**🔍 실습 질문**\n\njQuery의 '이 DOM을 이렇게 바꿔라'와 React의 '이 상태일 때 UI는 이렇다'는 어떻게 다른가?"
  },
  {
    "question": "44. 렌더와 커밋, 그리고 Fiber 아키텍처",
    "answer": "React는 **렌더 단계(UI 계산)**와 **커밋 단계(DOM 반영)**를 분리해요. 렌더 단계에서 컴포넌트 함수를 실행해 새 Virtual DOM을 만들고, 이전 결과와 **O(n) diffing**으로 비교한 뒤, 커밋 단계에서 실제로 변경된 DOM만 반영합니다. 여기서 핵심이 **Fiber 아키텍처**인데, 각 노드를 하나의 작업 단위로 쪼개서 렌더링을 **중단하고 재개**할 수 있게 해줘요. 덕분에 **time-slicing**과 우선순위 스케줄링이 가능합니다. 재조정 규칙은 간단한데, **타입이 다르면 하위 트리를 통째로 교체**하고, 같은 타입의 리스트 항목은 **key로 정체성을 추적**합니다.\n\n```ts\n상태 변경\n    ↓\n┌──────────────────────────────────────────────────┐\n│ [렌더 단계] — 순수 계산, 부수효과 금지            │\n│  컴포넌트 함수 실행 → 새 JSX 생성 → diffing       │\n│  ※ React 18에서는 중단/재개 가능 — 시간 분할(time-slicing)   │\n└──────────────────────────────────────────────────┘\n    ↓\n┌──────────────────────────────────────────────────┐\n│ [커밋 단계] — 실제 DOM 변경                       │\n│  변경된 DOM 노드만 업데이트                        │\n│    → useLayoutEffect (DOM 변경 직후, 동기)        │\n│      → 브라우저 페인트                             │\n│        → useEffect (페인트 이후, 비동기)           │\n└──────────────────────────────────────────────────┘\n```\n\n---\n\n**⚠️ 자주 하는 오해**\n\n- 리렌더가 일어나면 무조건 DOM도 다시 그린다 → **틀림.** 렌더 결과가 이전과 같으면 DOM 변경 없음.\n- Virtual DOM이 있으니 무조건 빠르다 → **틀림.** 비교 비용이 있다. 핵심은 속도보다 **선언형 UI를 가능하게 하는 추상화**.\n- Fiber는 그냥 Virtual DOM의 다른 이름이다 → **다르다.** Fiber는 중단/재개와 우선순위 스케줄링을 위한 구조.\n\n**🔍 실습 질문**\n\n컴포넌트가 렌더되었는데 화면이 안 바뀌었다. 가능한가? 이유는?"
  },
  {
    "question": "45. state ownership과 상태 끌어올리기",
    "answer": "**State ownership**은 어떤 상태를 어느 컴포넌트가 책임질지 정하는 문제예요. 형제 컴포넌트가 같은 상태를 각각 관리하면 쉽게 어긋나기 때문에, 공유가 필요하면 **가장 가까운 공통 부모**가 소유하고 props로 내려주는 게 안전합니다. 다만 무조건 높이 올리면 되는 게 아니라, 너무 높이 두면 **리렌더 범위가 불필요하게 넓어져요**. 그래서 **\"누가 읽고 누가 변경하는가\"**를 기준으로 가장 가까운 적절한 위치에 두는 판단이 중요합니다.\n\n> 상태 끌어올리기를 봤다. **너무 깊은 트리에서 props를 계속 내려줘야 하면?** Context로 넘어간다.\n\n```tsx\n// Bad: 같은 의미의 상태를 각각 관리 — 쉽게 어긋남\nfunction Sidebar() {\n  const [selectedId, setSelectedId] = useState(null);\n}\nfunction Detail() {\n  const [selectedId, setSelectedId] = useState(null);\n}\n\n// Good: 소유자를 하나로 정하고 나머지는 props로 받는다\nfunction Parent() {\n  const [selectedId, setSelectedId] = useState(null);\n  return (\n    <>\n      <Sidebar selectedId={selectedId} onSelect={setSelectedId} />\n      <Detail selectedId={selectedId} />\n    </>\n  );\n}\n```\n\n---\n\n**⚠️ 자주 하는 오해**\n\n- 공유되면 무조건 전역 상태다 → **틀림.** 공통 부모에 두는 것만으로 충분한 경우가 많다.\n- 가장 상위에 두면 관리가 쉽다 → **리렌더 범위가 불필요하게 넓어진다.**\n- 로컬 상태는 작고 단순한 경우에만 쓴다 → **오히려 많은 UI 상태는 로컬이 더 적합하다.**\n\n**🔍 실습 질문**\n\n두 컴포넌트가 같은 state를 써야 하면 가장 먼저 할 일은?"
  },
  {
    "question": "46. Context는 언제 유용하고, 언제 과용되기 쉬운가",
    "answer": "**Context**는 **props drilling을 줄여주는 값 전달 도구**인데, 가장 중요한 특성은 값이 바뀌면 `useContext`로 구독하는 **모든 컴포넌트가 리렌더**된다는 거예요. 그래서 테마, 로케일, 인증처럼 **변경 빈도가 낮은 공통 값**에 잘 맞고, 관련 없는 상태를 하나의 Context에 묶으면 불필요한 리렌더가 생기니까 **관심사별로 분리**해야 합니다. Provider의 value에 매번 새 객체를 넘기면 매 렌더마다 구독자가 리렌더되니 `useMemo`로 참조를 안정화해야 하고, 빈번히 바뀌는 상태나 **부분 구독**이 필요한 경우는 Zustand 같은 외부 도구가 더 적합합니다.\n\n> Context의 한계를 봤다. **그렇다면 Zustand/Redux 같은 외부 도구는 무엇이 다른가?**\n\n```tsx\nconst ThemeContext = createContext<\"light\" | \"dark\">(\"light\");\n\nfunction App() {\n  const [theme, setTheme] = useState<\"light\" | \"dark\">(\"light\");\n  return (\n    <ThemeContext.Provider value={theme}>\n      <Header />\n      <Main />\n    </ThemeContext.Provider>\n  );\n}\n\nfunction DeepChild() {\n  const theme = useContext(ThemeContext); // props drilling 없이 접근\n  return <div className={theme}>...</div>;\n}\n```\n\n---\n\n**⚠️ 자주 하는 오해**\n\n- Context만 쓰면 상태관리 문제는 끝난다 → **틀림.** Context는 \"값 전달\"이지 \"상태관리\"가 아니다.\n- Context를 쓰면 성능 걱정은 없다 → **값 변경 시 모든 구독자가 리렌더된다.**\n- 모든 공유 상태를 Context에 넣어야 한다 → **빈번히 변경되는 상태는 외부 도구가 더 적합하다.**\n\n**🔍 실습 질문**\n\nContext 값이 바뀌면 해당 Context를 구독하는 모든 컴포넌트가 리렌더되는데, 이를 줄이려면?"
  },
  {
    "question": "47. Context와 Zustand/Redux는 어떤 문제를 다르게 푸나",
    "answer": "Context는 **값 전달** 메커니즘이고, Zustand/Redux는 **상태 저장 + 구독 제어**에 초점이 있어요. 가장 큰 차이는 **selector로 부분 구독**이 가능한지인데, Context 값이 바뀌면 모든 구독자가 리렌더되는 반면 Zustand는 selector로 특정 slice만 구독해서 **불필요한 리렌더를 막을 수 있어요**. 실무에서 선택 기준을 정리하면, 서버 데이터는 **React Query**, 변경 빈도 낮은 공통값은 **Context**, 부분 구독이 필요하거나 변경이 잦으면 **Zustand**, 엄격한 액션 추적과 시간 여행 디버깅이 필요하면 **Redux Toolkit**이 적합합니다.\n\n> 클라이언트 상태 구조를 봤다. **서버에서 받아오는 데이터도 같은 방식으로 다뤄야 할까?**\n\n```tsx\n// Context: theme이 바뀌면 user만 쓰는 컴포넌트도 리렌더\nconst { user } = useContext(AppContext);\n\n// Zustand: selector로 user만 구독 → theme이 바뀌어도 리렌더 안 됨\nconst user = useStore((state) => state.user);\n```\n\n---\n\n**⚠️ 자주 하는 오해**\n\n- Zustand/Redux는 앱이 커야만 쓴다 → **부분 구독이 필요하면 작은 앱에서도 유용하다.**\n- Context로도 되는데 외부 도구가 불필요하다 → **구독 제어에서 차이가 크다.**\n- 외부 상태관리 도구를 쓰면 설계 고민이 사라진다 → **어떤 상태를 넣을지는 여전히 설계 문제.**\n\n**🔍 실습 질문**\n\nZustand에서 useStore(s => s.count)로 구독하면 왜 다른 상태가 바뀌어도 리렌더 안 되나?"
  },
  {
    "question": "48. client state와 server state는 왜 구분해야 하나",
    "answer": "**Client state**는 모달 열림, 탭 선택, 드래그 좌표처럼 **브라우저 안에서 완결되는 UI 상태**이고, **server state**는 서버에서 가져오는 데이터라서 **캐싱, stale 관리, background refetch, mutation 후 invalidation**이라는 추가 차원이 있어요. 둘을 구분해야 어떤 도구를 쓸지 명확해지는데, client state는 useState나 Zustand로, server state는 **React Query**로 관리하는 게 자연스럽습니다. React Query는 server state 패턴을 자동화해서 **staleTime/gcTime**으로 캐시 전략을 세우고, **optimistic update**로 체감 성능까지 제어할 수 있어요.\n\n> 상태의 큰 축을 나눴다. 이제 복잡한 상태 전환을 다루는 **useReducer**, 렌더링 우선순위를 제어하는 **concurrent features**, 그리고 구조 설계의 나머지 축들로 넘어간다.\n\n```tsx\nfunction UserProfile({ userId }) {\n  const [user, setUser] = useState(null);\n  const [loading, setLoading] = useState(true);\n  const [error, setError] = useState(null);\n\n  useEffect(() => {\n    setLoading(true);\n    fetch(`/api/users/${userId}`)\n      .then((res) => res.json())\n      .then(setUser)\n      .catch(setError)\n      .finally(() => setLoading(false));\n  }, [userId]);\n\n  // 문제점:\n  // 1. 매 컴포넌트마다 loading/error/data 3개 state 반복\n  // 2. 캐싱 없음 — 같은 userId를 다시 열면 또 fetch\n  // 3. stale 관리 없음 — 서버가 바뀌어도 모름\n  // 4. 중복 요청 — 여러 컴포넌트가 같은 데이터 요청\n  // 5. race condition 대응 — 직접 구현해야 함\n}\n```\n\n---\n\n**⚠️ 자주 하는 오해**\n\n- 서버에서 온 값도 다 같은 state다 → **캐싱, stale, 재요청이라는 추가 차원이 있다.**\n- React Query가 있으면 Zustand가 필요 없다 → **역할이 다르다.** 서버 데이터는 React Query, UI 상태는 Zustand/useState.\n- useEffect + fetch면 충분하다 → **규모가 커질수록 반복 코드와 누락이 많아진다.**\n\n**🔍 실습 질문**\n\nAPI에서 받은 사용자 목록은 서버 상태인가 로컬 상태인가? 이유는?"
  },
  {
    "question": "49. useReducer는 언제 useState 대신 쓰나",
    "answer": "**useReducer**는 다음 상태가 **이전 상태 + 액션**의 조합으로 결정될 때, 상태 전환 로직을 한 곳에 모아 관리하는 훅이에요. 여러 상태 값이 함께 변하거나 전환 조건에 분기가 많을 때 특히 유용한데, 리듀서 함수에 전환 규칙을 모아두면 컴포넌트는 `dispatch`만 호출하면 되니까 **로직과 UI가 깔끔하게 분리**됩니다. Immer와 함께 쓰면 중첩된 상태 업데이트도 간결하게 작성할 수 있어요.\n\n```tsx\ntype FormState = {\n  values: { name: string; email: string };\n  errors: { name?: string; email?: string };\n  isSubmitting: boolean;\n};\n\ntype FormAction =\n  | { type: \"SET_FIELD\"; field: string; value: string }\n  | { type: \"SUBMIT_START\" }\n  | { type: \"SUBMIT_SUCCESS\" }\n  | { type: \"SUBMIT_FAILURE\"; errors: Record<string, string> };\n\nfunction formReducer(state: FormState, action: FormAction): FormState {\n  switch (action.type) {\n    case \"SET_FIELD\":\n      return {\n        ...state,\n        values: { ...state.values, [action.field]: action.value },\n        errors: { ...state.errors, [action.field]: undefined }, // 입력 시 에러 클리어\n      };\n    case \"SUBMIT_START\":\n      return { ...state, isSubmitting: true };\n    case \"SUBMIT_SUCCESS\":\n      return { ...state, isSubmitting: false };\n    case \"SUBMIT_FAILURE\":\n      return { ...state, isSubmitting: false, errors: action.errors };\n  }\n}\n```\n\n---\n\n**⚠️ 자주 하는 오해**\n\n- useReducer는 Redux의 축소판이다 → **비슷한 패턴이지만 미들웨어, 전역 스토어 개념이 없다.**\n- 복잡한 앱에서만 쓴다 → **상태 2~3개가 함께 바뀌는 경우에도 유용하다.**\n\n**🔍 실습 질문**\n\nuseState vs useReducer — 폼 필드가 10개인 복잡한 폼에서는 어떤 게 적합한가?"
  },
  {
    "question": "50. React 18 concurrent features",
    "answer": "React 18의 **concurrent features**는 렌더링에 **우선순위**를 부여해서 급한 업데이트가 덜 급한 렌더링을 기다리지 않게 해줘요. `useTransition`은 상태 업데이트를 **non-urgent**로 표시해서 입력 응답성을 유지하고, `useDeferredValue`는 값의 반영을 지연시켜 비싼 렌더링을 뒤로 미룹니다. **Suspense**는 비동기 데이터의 로딩 상태를 선언적으로 처리하고요. 핵심은 렌더 자체가 빨라지는 게 아니라 **우선순위 제어로 체감 응답성이 좋아지는 것**이에요.\n\n```tsx\nfunction SearchPage() {\n  const [query, setQuery] = useState(\"\");\n  const [filterQuery, setFilterQuery] = useState(\"\");\n  const [isPending, startTransition] = useTransition();\n\n  const handleChange = (e) => {\n    setQuery(e.target.value);          // urgent — 입력 즉시 반영\n    startTransition(() => {\n      setFilterQuery(e.target.value);  // non-urgent — 리스트 필터링은 나중에\n    });\n  };\n\n  return (\n    <div>\n      <input value={query} onChange={handleChange} />\n      {isPending && <span>검색 중...</span>}\n      <HeavyList filterQuery={filterQuery} />\n    </div>\n  );\n}\n```\n\n**🔍 실습 질문**\n\nReact 18의 startTransition은 어떤 문제를 해결하나?"
  },
  {
    "question": "51. 커스텀 훅 — 로직 재사용의 기본 단위",
    "answer": "**커스텀 훅**은 상태와 효과를 묶어서 **재사용 가능한 단위로 추출**하는 함수예요. 여러 곳에서 같은 로직이 반복되거나 컴포넌트가 UI보다 로직이 길어질 때 분리하는데, `use` 접두사로 시작하며 훅 규칙(조건문 안에서 호출 금지 등)을 따릅니다. 한 곳에서만 쓰더라도 **읽기 쉬움과 책임 분리** 측면에서 충분히 가치가 있어요.\n\n```tsx\n// 재사용 가능한 디바운스 훅\nfunction useDebounce<T>(value: T, delay: number): T {\n  const [debounced, setDebounced] = useState(value);\n  useEffect(() => {\n    const id = setTimeout(() => setDebounced(value), delay);\n    return () => clearTimeout(id);\n  }, [value, delay]);\n  return debounced;\n}\n```\n\n**🔍 실습 질문**\n\n커스텀 훅 useDebounce(value, 300)은 내부적으로 어떤 훅을 조합하나?"
  },
  {
    "question": "52. 복잡한 UI에서는 상태를 어떻게 나눠야 하나",
    "answer": "복잡한 UI에서는 상태를 **영속(DB 반영), 일시(hover/drag), 파생(계산 가능), 서버(동기화 필요)**로 나눠서 역할별로 관리하는 게 핵심이에요. 특히 **파생 상태를 useState + useEffect로 동기화하는 건 대표적인 안티패턴**이고, `useMemo`나 일반 변수로 충분합니다. 저장 대상은 React Query 같은 서버 상태 도구, 순간 상태는 로컬 useState, 계산 가능한 값은 렌더 중 직접 계산하면 되고요. 중첩된 데이터 구조는 **정규화**하면 특정 아이템만 업데이트할 때 나머지 참조가 유지되어 `React.memo` 같은 메모이제이션이 제대로 효과를 발휘합니다.\n\n```tsx\n// Bad: 파생 상태를 useState + useEffect로 동기화\nconst [filteredItems, setFilteredItems] = useState([]);\nuseEffect(() => {\n  setFilteredItems(items.filter((i) => i.category === selected));\n}, [items, selected]);\n// 불필요한 state + effect + 한 틱 지연 + 동기화 버그 가능\n\n// Good: useMemo 또는 렌더 중 직접 계산\nconst filteredItems = useMemo(\n  () => items.filter((i) => i.category === selected),\n  [items, selected],\n);\n// state가 아니라 계산값 — 동기화 문제 없음\n```\n\n---\n\n**⚠️ 자주 하는 오해**\n\n- 상태를 한 객체에 모으면 관리가 쉽다 → **규모가 커지면 반대.**\n- 파생값도 전부 state로 둬야 한다 → **계산으로 충분. useState + useEffect 동기화는 안티패턴.**\n- 드래그 좌표도 전역이 낫다 → **일시 상태는 가장 좁은 범위가 적절.**\n\n**🔍 실습 질문**\n\n모달 open/close, 드래그 좌표, 서버 데이터 — 이 세 가지를 어떤 상태 도구로 각각 관리하겠나?"
  }
]