[
  {
    "question": "77. 테스트는 왜 \"변경 자신감\"과 연결되나",
    "answer": "테스트의 본질적 가치는 단순한 버그 탐지가 아니라 **변경 자신감**입니다. 코드를 바꿔도 기존 동작이 보장된다는 안전망이 있으면, 리팩토링이나 기능 추가를 과감하게 진행할 수 있습니다. 반대로 테스트가 없으면 수정 자체가 무서워져 팀 속도가 느려지고, 배포 전 수동 확인에 과도한 시간을 쓰게 됩니다.\n\n**🔍 실습 질문**\n\n테스트 없이 3개월 미룬 리팩터링을 테스트 작성 후 하루 만에 끝낸 사례의 핵심 교훈은?"
  },
  {
    "question": "78. unit, integration, E2E — 테스트 계층",
    "answer": "**Unit** 테스트는 순수 함수 같은 작은 로직의 정확성을, **integration** 테스트는 컴포넌트와 상태와 이벤트가 연결되는 흐름을, **E2E** 테스트는 실제 브라우저에서 사용자 시나리오 전체를 검증합니다. 프론트엔드에서는 \"각 함수는 맞는데 연결하면 안 되는\" 버그가 많기 때문에 **integration 비중이 특히 중요**하고, E2E는 비용이 크므로 로그인이나 결제 같은 핵심 흐름에 집중하는 것이 효율적입니다.\n\n```ts\n╱╲\n         ╱  ╲          E2E (End-to-End)\n        ╱ E2E╲         느림 · 비쌈 · 높은 신뢰도\n       ╱──────╲        실제 브라우저에서 사용자 시나리오\n      ╱        ╲\n     ╱Integration╲     Integration (통합)\n    ╱──────────────╲    중간 속도 · 컴포넌트 간 상호작용\n   ╱                ╲\n  ╱    Unit Tests    ╲  Unit (단위)\n ╱────────────────────╲  빠름 · 저렴 · 격리된 로직 검증\n╱                      ╲\n\n← 많이                     적게 →  (테스트 수)\n← 빠름                     느림 →  (실행 속도)\n← 낮음                     높음 →  (신뢰도)\n```\n\n**🔍 실습 질문**\n\n단위 테스트가 다 통과하는데 실제 화면에서 버그가 나는 이유는?"
  },
  {
    "question": "79. 테스트 ROI — 어디에 투자할 것인가",
    "answer": "테스트 **ROI**는 커버리지 숫자가 아니라 \"깨졌을 때 얼마나 아픈가, 변경이 얼마나 잦은가, 수동으로 확인하는 데 비용이 얼마나 큰가\"로 판단합니다. 커버리지가 100%여도 결제나 인증 같은 중요한 시나리오가 빠져 있으면 의미가 없고, 반대로 거의 변하지 않는 설정 코드에 테스트를 잔뜩 두는 것은 ROI가 낮습니다. **인증, 결제, 공용 컴포넌트**처럼 실패 비용이 크고 변경이 잦은 곳에 우선 투자해야 합니다.\n\n**🔍 실습 질문**\n\n결제 로직 vs 정적 텍스트 — 테스트 ROI가 높은 쪽은? 이유는?"
  },
  {
    "question": "80. mock과 API mocking",
    "answer": "**mock**은 외부 의존성을 제어 가능한 가짜로 대체해서 테스트를 빠르고 안정적으로 만들어주지만, 과도하게 쓰면 실제 구현과 너무 멀어져서 \"테스트는 통과하는데 실제론 깨지는\" 위험한 상태가 됩니다. API mocking에서는 **MSW**가 네트워크 레벨에서 요청을 가로채기 때문에 fetch나 axios를 직접 mock하는 것보다 훨씬 현실적입니다. unit/integration에서는 MSW를 적극 활용하되, 핵심 E2E는 스테이징 같은 실제에 가까운 환경에서 돌리는 균형이 중요합니다.\n\n```ts\n// mocks/handlers.ts\nimport { http, HttpResponse } from \"msw\";\n\nexport const handlers = [\n  http.get(\"/api/users/:id\", ({ params }) => {\n    return HttpResponse.json({ id: params.id, name: \"홍길동\" });\n  }),\n];\n```\n\n**🔍 실습 질문**\n\nmock이 실제 API와 달라도 테스트가 통과하는 문제를 어떻게 방지하나?"
  },
  {
    "question": "81. flaky test와 비동기 UI 테스트",
    "answer": "**Flaky test**는 코드 변화 없이 결과가 들쭉날쭉한 테스트로, 누적되면 팀이 빨간불을 무시하게 되어 자동화 전체의 가치가 무너집니다. 가장 흔한 원인은 `setTimeout`으로 대충 기다리는 패턴인데, 대신 **waitFor**나 **findBy**로 상태 변화 조건을 명시적으로 기다려야 안정적입니다. 테스트 간 독립성을 보장하고, 네트워크는 MSW로 통제하며, CI에서 flaky를 별도로 추적해서 격리하고 기한 내에 수정하거나 삭제하는 운영 정책을 두는 것이 중요합니다.\n\n```tsx\n// BAD: setTimeout으로 대충 기다리기\nawait new Promise((r) => setTimeout(r, 500));\nexpect(screen.getByText(\"홍길동\")).toBeInTheDocument();\n\n// GOOD: 상태 변화를 명시적으로 기다리기\nawait waitFor(() => expect(screen.getByText(\"홍길동\")).toBeInTheDocument());\n\n// GOOD: findBy 사용 (waitFor + getBy 축약)\nexpect(await screen.findByText(\"홍길동\")).toBeInTheDocument();\n```\n\n**🔍 실습 질문**\n\n같은 테스트가 CI에서 10번 중 2번 실패한다. 가장 먼저 확인할 것은?"
  },
  {
    "question": "82. 복잡한 프론트엔드 시나리오 테스트",
    "answer": "업로드, **optimistic update**, 세션 만료 같은 복잡한 시나리오는 성공하는 happy path만 테스트하면 부족합니다. 실패 시 롤백이 제대로 되는지, 중복 요청은 어떻게 처리되는지, 취소 흐름은 정상 동작하는지까지 **상태 전이와 예외 흐름을 함께 검증**해야 실제 안정성이 높아집니다. 이런 시나리오는 수동 테스트로는 반복 회귀를 놓치기 쉬우므로 자동화가 더욱 중요합니다.\n\n```tsx\n// optimistic update: 성공 + 실패 롤백\nit(\"좋아요 클릭 후 서버 실패하면 롤백\", async () => {\n  server.use(http.post(\"/api/like\", () => new HttpResponse(null, { status: 500 })));\n\n  render(<LikeButton initialCount={10} />);\n  await userEvent.click(screen.getByRole(\"button\", { name: \"좋아요\" }));\n\n  expect(screen.getByText(\"11\")).toBeInTheDocument();  // optimistic\n  await waitFor(() => expect(screen.getByText(\"10\")).toBeInTheDocument()); // 롤백\n});\n```\n\n**🔍 실습 질문**\n\nCSV 업로드 테스트에서 성공 케이스보다 실패 케이스가 더 중요한 이유는?"
  },
  {
    "question": "83. 테스트와 CI — 팀 품질 게이트",
    "answer": "테스트가 **CI**에 연결되면 개인 습관이 아니라 **팀 공통 품질 게이트**가 됩니다. PR마다 빠른 lint, type check, unit, 핵심 integration을 돌려서 즉각 피드백하고, main 병합 시에는 넓은 integration과 핵심 E2E smoke 테스트를, 야간에는 전체 E2E와 브라우저 매트릭스를 돌려 안정성을 확보합니다. 느린 테스트는 병렬화로, flaky test는 격리 후 기한 내 수정이나 삭제로 별도 관리해야 CI 신뢰가 유지됩니다.\n\n```yaml\n# .github/workflows/test.yml (PR 단위)\nname: Test\non:\n  pull_request:\n    branches: [main]\njobs:\n  test:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with: { node-version: 20, cache: \"npm\" }\n      - run: npm ci\n      - run: npm run lint && npm run type-check\n      - run: npm test -- --coverage\n```\n\n**🔍 실습 질문**\n\n모든 E2E를 모든 PR에서 돌리면 어떤 문제가 생기나?"
  }
]